{
  "name": "@gf/api",
  "version": "0.0.0",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "tsconfig": "workspace:^"
  },
  "dependencies": {
    "@repo/axios": "workspace:^"
  }
}
